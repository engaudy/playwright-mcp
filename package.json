{
  "name": "playwright-api-tests",
  "version": "1.0.0",
  "description": "API test automation with Playwright and TypeScript",
  "scripts": {
    "test": "playwright test",
    "test:report": "playwright test --reporter=html && playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.44.0",
    "typescript": "^5.4.0"
  }
}
